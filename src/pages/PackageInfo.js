import React from 'react';
import PackageList from '../components/PackageList';

function PackageInfo() {
    return (
      <div>
        <PackageList/>
        {/* <h1>This is your Package</h1> */}
      </div>
    );
  }
  
  export default PackageInfo;